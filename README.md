# Exam-PDF-Personalizer

Upload a roster

Upload an exam booklet stack

Run the following code to install the libraries: pip install PyMuPDF pandas

Run the following code to create the personalized exams: python personalize_exams.py

